#include "main.h"

/**
 * print_line - Draws a straight line in the terminal.
 * @n: THe number of times the the character should be printed
 * Return: Always 0
 */
void print_line(int n)
{
	int i;

	if (n <= 0)
	{
		_putchar('\n');
	}
	else
	{
		for (i = o; i < n; i++)
		{
			_putchar('_')
		}
	}
}
