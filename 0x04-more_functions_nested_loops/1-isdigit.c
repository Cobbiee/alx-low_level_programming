#include "main.h"

/**
 * _isdigit - Checks for a digit through 0 to 9.
 * @c: The character that checks for the digits.
 * Return: 1 if it is a digit, 0 otherwise.
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
